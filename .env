# Docker Compose environment overrides for numbers-game
# External host port exposed on localhost
HOST_PORT=3210

# Internal container app settings
HOST=0.0.0.0
PORT=5000
GUNICORN_WORKERS=2

# Optional Flask direct-run fallback (unused by Gunicorn)
FLASK_DEBUG=false
